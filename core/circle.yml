machine:
  ruby:
    version: 1.9.3-p392
  services:
    - elasticsearch
test:
  override:
    - bundle exec rspec spec-unit --trace
    - bundle exec rspec spec --trace
    - bundle exec rake konacha:load_poltergeist konacha:run --trace
    - bundle exec rspec spec/acceptance --trace
    - bundle exec rspec spec/screenshots
