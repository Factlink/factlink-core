development:
    proxy:
        hostname: localhost
        port: 8080
        protocol: http://

test:
    proxy:
        hostname: localhost
        port: 8080
        protocol: http://

testserver:
    proxy:
        hostname: proxy.testserver.factlink.com
        port: 8080
        protocol: https://

production:
    proxy:
        hostname: proxy.factlink.com
        port: 8080
        protocol: https://