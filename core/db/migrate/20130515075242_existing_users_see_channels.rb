class ExistingUsersSeeChannels < Mongoid::Migration
  def self.up
    puts "old migration doesn't do anything anymore"
  end

  def self.down
    puts "old migration doesn't do anything anymore"
  end
end
