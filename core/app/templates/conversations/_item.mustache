<img src="{{ first_recipient.avatar_url_48 }}" class="message-avatar">

<div class="message">
  <span class="conversations-overview-user-link">{{ recipients_comma }}</span>
  <span class="message-meta">{{ last_message.time_ago }}</span>
  <div class="conversation-text">
    {{#reply}}
      <i class="icon-share-alt"></i>
    {{/reply}}
    {{ last_message.content }}
  </div>
</div>
