{{^is_current_user}}
<div class="avatar-container">
  <div class="user-avatar">
  	{{{ avatar }}}
  </div>

  <div class="wrapper">
  	<h3>{{ username }}</h3>
  	<a class="btn btn-mini" href="/{{ username }}">View profile</a>
  </div>
</div>
{{/is_current_user}}

{{#is_current_user}}
<h2>{{ channel_listing_header }}</h2>
{{{new_channel_button}}}
<a href="/{{ username }}/channels/new" class="btn btn-mini add-channel">Add new</a>
{{/is_current_user}}
