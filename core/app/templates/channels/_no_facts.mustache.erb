{{#is_mine}}
  {{#is_all}}
    <p>You have not created a {{ global.t.channel }} or added someone's {{ global.t.channel }} yet. You can:</p>
    <div class="empty-channel-action">
      <h2>Add a Factlink with Extension</h2>
      <a class="download-chrome-extension">
        <img src="<%= image_path("screenshots/addfactlink.png") %>">
      </a>
    </div>
    <div class="empty-channel-action">
      <h2>Create a {{global.t.channel}}</h2>
      <a href="{{add_channel_url}}">
        <img src="<%= image_path("screenshots/createchannel.png") %>">
      </a>
    </div>
    <div class="empty-channel-action-end"></div>
  {{/is_all}}
  {{#is_created}}
    <p>You have not created any Factlinks yet. You can create Factlinks anywhere on the web.</p>
    <h3>Add a Factlink with Extension</h3>
    <a href="{{global.chrome_extension_url}}">
      <img src="<%= image_path("screenshots/addfactlink.png") %>">
    </a>
  {{/is_created}}
  {{#is_normal}}
    <p>This {{ global.t.channel }} does not contain any Factlinks yet. Create a new Factlink above<span class="should-install-chrome-extension"> or install the extension and add Factlinks across the web</span>:</p>
    <div class="span3 related-channels"></div>

    <!-- Chrome extension -->
    <div class="span5 empty-channel-action should-install-chrome-extension visible-when-chrome">
      <h2>Download the Chrome extension</h2>
      <a class="btn download-chrome-extension"><%= image_tag("browser-icons/chrome-16x16.png", style: "margin: -2px 8px 0 0") %>Install Chrome Extension</a>
    </div>

    {{#global.can_haz.firefox_extension}}
    <!-- Firefox extension -->
    <div class="span5 empty-channel-action should-install-firefox-extension visible-when-firefox">
      <h2>Use the extension</h2>
      <p>Use the extension to enable Factlink on all the pages you visit.</p>
      <a id="firefox-extension-button" class="btn" href="<%= "#{FactlinkUI::Application.config.static_url}/extension/firefox/factlink-latest.xpi" %>"><%= image_tag("browser-icons/firefox-16x16.png", style: "margin: 0px 8px 0 0") %>Install Firefox Extension</a>
    </div>
    {{/global.can_haz.firefox_extension}}

    <div class="empty-channel-action-end"></div>
  {{/is_normal}}
{{/is_mine}}
{{^is_mine}}
  <p>This channel is currently empty</p>
{{/is_mine}}
