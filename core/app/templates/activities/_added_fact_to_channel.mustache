<a href="{{ user_profile_url }}" rel="backbone">{{{ avatar }}} {{ username }}</a> posted to <a href="{{ activity.channel_url }}">{{ activity.channel_title }}</a>

<span class="meta">{{ time_ago }}</span>

<div class="fact-block"></div>
