<div class="activities-header">
  <div class="activities-more">
    <a href="{{link}}" class="activities-more-button js-activities-refresh">
      <i class="icon-list"></i>
      <strong class="unread_count">{{unread_count}}</strong> new activities (click here to refresh)
    </a>
  </div>
</div>

<div class="js-activities-list">
</div>

<div class="empty-stream js-empty-stream"></div>
