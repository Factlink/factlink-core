<!-- fact listing -->

<% unless facts.count > 0 %>
	<p>No facts added yet.</p>
<% else %>
<ul id="fact-listing">
	<% facts.each do |fact| %>
		<li><%=	fact_snippet(fact) %></li>
	<% end %>
</ul>
<% end %>