<% if alert || notice %>
<div class="alert-message <%= alert ? "error" : "warning" %>">
  <a class="close" href="#">x</a>
  	<% if notice %>
			<%= notice %>
		<% end %>
		<% if alert %>
			<%= alert %>
	<% end %>
</div>
<% end %>
