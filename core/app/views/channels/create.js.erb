if (Channels !== undefined && "<%=current_user.graph_user.id%>" === Channels.models[0].get('created_by_id') ) {
  Channels.add(<%= Channels::SingleMenuItem.for_channel_and_view(@channel,self).to_json.html_safe %>);
}
