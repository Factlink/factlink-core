<% content_for :javascript do %>
  <script>
    $(function(){
      var channel = Channels.get(<%= @channel.id %>);
      
     channel.trigger('activate', channel);
     
     Router.view.channelView = new ChannelView({ model: channel }).render();
    });
  </script>
<% end %>