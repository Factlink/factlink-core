<% sentry_conf = YAML::load_file(Rails.root.join('config/sentry.yml'))[Rails.env].andand['ravenjs'] %>

<% if sentry_conf %>

  <script type="text/javascript">
  Raven.config('<%= "#{sentry_conf["protocol"]}://#{sentry_conf["public"]}@sentry.factlink.com/#{sentry_conf["project_id"]}" %>');

  window.onerror = Raven.process;
  </script>

<% end %>
