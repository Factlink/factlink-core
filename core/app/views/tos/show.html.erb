<div id="tos">
  <%= render "terms_of_service" %>
  <% if current_user %>
  <div class="center">
    <%= link_to("Go back to your profile", user_profile_path(current_user)) %>
  </div>
  <% end %>
</div>
