<h2 class="activity-title">
	<%= link_to_possibly_dead_user @activity.user.user %> 
	added your <%= @activity.subject.title %> 
	<%= t :channel %> 
	to their own 
	<%= link_to @activity.object.title, channel_url(@activity.object.created_by.user, @activity.object.id) %>.
</h2>
<p class="activity-action">
	Too see their <%= @activity.object.title %> channel, visit:<br>
	<%= link_to channel_url(@activity.object.created_by.user, @activity.object.id), channel_url(@activity.object.created_by.user, @activity.object.id) %>
</p>

