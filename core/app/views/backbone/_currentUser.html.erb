<% if current_user and not current_user.features.include? :act_as_non_signed_in %>
  window.currentUser = new User(<%= render('users/user', user: current_user).html_safe %>);
  chans = new OwnChannelCollection([]);
  window.currentUser.setChannels(chans);
  chans.fetch();
<% end %>
