<li>
  <%= link_to("#{image_tag(current_user.avatar_url(size:30), class: 'image-30px')} #{h current_user.name}".html_safe, user_profile_path(current_user), class: 'potential-backbone topbar-profile-link') %>
</li>
