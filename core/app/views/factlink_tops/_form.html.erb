<%= form_for(@factlink_top) do |f| %>
  <% if @factlink_top.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factlink_top.errors.count, "error") %> prohibited this factlink_top from being saved:</h2>

      <ul>
      <% @factlink_top.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Displaystring:" %>
    <%= f.text_field :displaystring %>
  </div>

  <div class="field">
    <%= f.label "Site:" %>
		<%= select_tag(:site_id, options_from_collection_for_select(Site.all, :id, :url)) %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
