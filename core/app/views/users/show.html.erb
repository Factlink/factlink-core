<!-- header -->
<% content_for :header do %>

<% end %>


<!-- left column -->
<% content_for :left_column do %>
	
	<ul>
		<%= user_block(@user) %>
	</ul>
	
	<%= channel_listing_for_user(@user.channels, @user) %>

<% end %>


<!-- main column -->
<% content_for :main_column do %>

	<% if @user.channels.count > 0 %>
		<%= fact_listing_for_channel(@user.channels.first) %>
	<% end %>

<% end %>