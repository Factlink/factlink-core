<div id="activity-listing">

	<h1><%= user.username %></h1>

	<ul id="activity-list">
	<% user.activities.sort(:order => "DESC")[0..(nr - 1)].each do |activity| %>
		<%= show_activity(activity) %>
	<% end %>
	</ul>

</div>