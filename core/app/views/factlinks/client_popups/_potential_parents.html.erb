<div id="fl-potential-parents-popup" class="fl-potential-popup"
style="position: absolute; 
border: solid 10px rgba(0, 0, 0, 0.2);
border-radius: 4px; padding: 4px;
width: 390px;
height: 248px;
background: #eee;
box-shadow: 0px 0px 8px #888;
top: 60px;
left: 10px;
z-index: 24;
display: none;">
	
	<%= image_tag "icons/close-button.png", :class => "close-button", :style => "position: absolute; top: -18px; left: 386px;", :onclick => "$('#dimmer').fadeOut('fast'); $('#fl-potential-parents-popup').fadeOut('fast');" %>
	<span style="margin: 4px;">Select a fact to add this fact as source:</span>

	<div class="fl-scroll-pane" style="margin-top: 4px;">
		<ol id="fl-existing-parents-list" class="fl-sources" style="height: 200px;">
			
			
			<% unless potential_parents.count > 0 %>

				<li id="no-facts-message">						
					<span class="fl-statement-title">No facts have been added yet.</span>
				</li>
			
			<% else %>

				<% potential_parents.each do |parent| %>
				<li id="fl-source-<%= parent.id %>">

					<div style="width: 320px; margin-left: -20px;">
						<span><%= parent.displaystring %></span>
						<p><%= parent.content || "<no content available>" %></p>
						<% if parent.site %>
							<span class="fl-source">found on <a href="<%= parent.site.url %>" target="_blank"><%= parent.site.url %></a></span>
						<% else %>
							<span class="fl-source">found on <a href="<%= parent.url %>" target="_blank"><%= parent.url %></a></span>
						<% end %>
					</div>

					<div style="position: absolute; right: 10px; top: 30px;">
						<%= link_to('add', add_factlink_to_parent_path(@factlink, parent), :remote => true, :class => "button", :style => "color: #000; padding: 4px; text-decoration: none;" )%>
					</div>
				</li>
				<% end %>
			<% end %>
		</ol>
	</div>
	
	<div class="center" style="padding: 8px;">
		<a onclick="javascript: $('#dimmer').fadeOut('fast'); $('#fl-potential-parents-popup').fadeOut('fast');">or cancel</a>
	</div>
	
</div>