<%= form_for(@factlink) do |f| %>
  <% if @factlink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factlink.errors.count, "error") %> prohibited this factlink from being saved:</h2>

      <ul>
      <% @factlink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
