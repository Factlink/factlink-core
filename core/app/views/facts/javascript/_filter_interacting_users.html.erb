<script>
	function apply_filter(klass) {
		$('ol#fl-users > li').fadeOut(40).filter(klass).fadeIn(40);

		// override for the 'all' filter
		if (klass == "*") {
			klass = "all";
		}
		
		$('div#filters > a').removeClass('active').filter('.' + klass).addClass('active');
	}
</script>