</td><td class="evidence-options">

  <% @labelClass = fact_relation.type == "supporting" ? "notice" : "warning" %>
  
  <div class="relevance-indicator">
    <span class="label <%= @labelClass %>"><%= fact_relation.type %></span>
  </div>

  <div class="relevance-options">
    <ul>
      <% if user_signed_in? %>
          <%= evidence_buttons(fact_relation,current_user )%>
      <% end %>
    </ul>
  <div>
</td><td>

<%= render_mustache "/facts/fact_bubble", :fact => fact_relation.from_fact, :form_authenticity_token => form_authenticity_token %>