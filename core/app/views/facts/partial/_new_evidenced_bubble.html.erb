</td><td class="evidence-options">
  <div class="relevance-options">
    <ul>
      <% if user_signed_in? %>
          <li><%= link_to('add supported', add_supporting_evidenced_path(fact.id, add_to_fact.id), :remote => true)%></li>
          <li><%= link_to('add weakened',	 add_weakening_evidenced_path(fact.id, add_to_fact.id),  :remote => true)%></li>
      <% end %>
    </ul>
  <div>
</td><td>
<%= render :partial => "/facts/partial/fact_bubble", :locals => {:fact => fact} %>
