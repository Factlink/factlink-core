<style>
	div#fl-relevance-container span {
		padding: 2px 12px;
	}

	div#fl-relevance-container span.active { 
			border: solid 1px white;
			background-color: #888;
			color: white;
			border-radius: 16px;
			text-decoration: none;
			text-shadow: nonefactlink;
	}

</style>

TODO: Check what the users opinion on the FactRelation is.

<%= link_to("<span class=\"relevant #{relevance_class_on_fact_relation(fact_relation, :beliefs)}\">Relevant</span>".html_safe, relevance_path(evidence.id, :relevant, fact.id), :remote => true) %>
<%= link_to("<span class=\"might-be-relevant #{fact_relation.relevance_class(:doubts, current_user)}\">Might be relevant</span>".html_safe, relevance_path(evidence.id, :might_be_relevant, fact.id), :remote => true) %>
<%= link_to("<span class=\"not-relevant #{fact.relevance_class(:disbeliefs, current_user)}\">Not relevant</span>".html_safe, relevance_path(evidence.id, :not_relevant, fact.id), :remote => true) %>

