<ul>	
	<li<%= active_class_for_type_and_fact(:beliefs, fact_relation.from_fact) %>>
		<%= link_to("<span>agree</span>".html_safe, opinion_path(fact_relation.id, :beliefs), :remote => true) %>
	</li>

	<li<%= active_class_for_type_and_fact(:doubts, fact_relation.from_fact) %>>
		<%= link_to("<span>doubt</span>".html_safe, opinion_path(fact_relation.id, :doubts), :remote => true) %>
	</li>
	
	<li<%= active_class_for_type_and_fact(:disbeliefs, fact_relation.from_fact) %>>
		<%= link_to("<span>disagree</span>".html_safe, opinion_path(fact_relation.id, :disbeliefs), :remote => true) %>
	</li>
</ul>