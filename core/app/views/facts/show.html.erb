<%= render "/facts/fact", :fact => @fact, :channel => nil, :modal => @modal, :hide_links_for_site => @hide_links_for_site %>

<% content_for :javascript do %> 

  <script>
    $('.fact-block').factlink();
    window.fact_id = "<%= @fact.id %>";
  </script>
<% end %>
