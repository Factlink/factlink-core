# ensure we update this file when we update the locales
# SHASUM of locales: efdfaf0efb82c7722cb0f831daf0e61ca92be323

window.Kennisland = <%=FactlinkUI.Kennisland?.to_json %>;

window.Factlink ||= {}

Factlink.Global =
  ajax_loader_image: '<%= image_path("ajax-loader.gif") %>'
  bookmarklet_link: null # Set in bookmarklet.coffee.erb

  mixpanel_token: '<%= FactlinkUI::Application.config.mixpanel_token %>'
  facebook_app_id: '<%= FactlinkUI::Application.config.omniauth_conf['facebook']['id'] %>'

  core_url: "<%= URI.parse(FactlinkUI::Application.config.core_url).to_s %>"
  environment: "<%= Rails.env %>"
  t: {}
  signed_in: false

<%
  [
    :follow_user,
    :followed,
    :following,
    :unfollow,

    :post_comment,
    :post_subcomment,
    :factlinks
  ].each do |translation|
%>

Factlink.Global.t.<%= translation.to_s %> = "<%= I18n.t(translation) %>"

<% end %>
