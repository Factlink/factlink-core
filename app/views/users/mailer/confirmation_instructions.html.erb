<% content_for :email_header do %>
  <h1 class="header-title">Hello <%= @resource.full_name %></h1>
  <h2 class="header-subtitle">Welcome to <%= t(:app_name) %>! Please confirm your email by clicking below.</h2>
<% end %>

<% content_for :email_body do %>

  <table class="button" align="center" border="0" cellpadding="0" cellspacing="0">
    <tr>
      <td class="button-inner">
        <%= link_to confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) do %>
          <div class="button-link">
            Confirm email address
          </div>
        <% end %>
      </td>
    </tr>
  </table>

  <br /><br />
  <p>We’ve only built part of our vision of what <%= t(:app_name) %> could be. Please send us your <a href="mailto:<%= FactlinkUI::Application.config.support_email %>">feedback</a> so we can make it better!</p>

  <p><em>&mdash; The <%= t(:app_name) %> team</em></p>

<% end %>

<%= render template: "layouts/email" %>
